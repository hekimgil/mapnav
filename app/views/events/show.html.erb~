<p id="notice"><%= notice %></p>

<% id = params[:id] %>
<% e = Event.find(id) %>
<% u = User.find(e[:ownerUser]) %>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <tr><td>Event: </td><td> <%= e[:title] %></td></tr>
    <tr><td>Owner: </td><td> <%= u.firstName + " " + u.lastName %></td></tr>
    <tr><td>Title: </td><td> <%= e[:title] %></td></tr>
    <tr><td>Description: </td><td> <%= e[:description] %></td></tr>
    <tr><td>Location: </td><td> <%= e[:latitude] %>, <%= e[:longitude] %></td></tr>
    <tr><td>Date: </td><td> <%= e[:createdDate] %></td></tr>
    <tr><td></td><td><img src="<%= e[:thumbnail] %>" alt="e[:title]" height="200" width="200"></td></tr>
  </tbody>
</table>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @photos.each do |photo| %>
      <tr>
        <td><%= id %></td>
        <td><%= photo[:title] %></td>
        <td><%= photo[:createdDate] %></td>
        <td><img src="<%= photo[:thumbnail] %>" alt="photo[:title]" height="50" width="50"></td>
        <td><%= link_to 'Show', photo %></td>
        <td><%= link_to 'Edit', edit_photo_path(photo) %></td>
#        <td><%= link_to 'Destroy', photo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<%= link_to 'Edit', edit_event_path(@event) %> |
<%= link_to 'Back', events_path %>
